# テスト項目書

**プロジェクト名:** SES-RAG-DEMO
**最終更新日:** 2026-05-19

**凡例**

| 記号 | 意味 |
|--------|---------|
| 優先度:H / M / L | 高 / 中 / 低 |
| 種別:UT / IT / ST | ユニットテスト / 結合テスト / システムテスト |
| 結果:✔︎ / △ / ❌ / - | 合格 / 条件付き合格 / 不合格 / 未実施 |

---

## 1. embedder.py テストケース

| No. | テスト内容 | 優先度 | 種別 | 結果 |
|-----|------------------|----------|------|--------|
| E-01 | `engineers.json` が正常にロードされ、20件のリストとして返却される | H | UT | ✔︎ |
| E-02 | `build_profile_text()` がエンジニア辞書を受け取り、名前・スキル・経験年数を含む文字列を返却する | H | UT | ✔︎ |
| E-03 | `build_index()` が正常完了時に登録件数(int)を返却する(Embeddingをモック化) | H | IT | ✔︎ |
| E-04 | `build_index()` を2回実行しても重複登録されず、コレクションが上書き(再構築)される | M | IT | ✔︎ |
| E-05 | `engineers.json` の存在しないパスを渡した際に適切な例外(`FileNotFoundError` 等)が発生する | M | UT | ✔︎ |

---

## 2. retriever.py テストケース

| No. | テスト内容 | 優先度 | 種別 | 結果 |
|-----|------------------|----------|------|--------|
| R-01 | `search_engineers()` が文字列を受け取りリストを返却する(ChromaDBインデックス構築済の状態で実行) | H | IT | ✔︎ |
| R-02 | `top_k=3` 指定時、返却リストに3件含まれる | H | IT | ✔︎ |
| R-03 | 返却リストの各要素に `id` / `document` / `distance` / `metadata` が含まれる | H | IT | ✔︎ |
| R-04 | すべての distance が 0.0〜2.0 の範囲に収まる(ChromaDBの距離指標の範囲確認) | M | IT | ✔︎ |
| R-05 | Pythonバックエンド案件要件を入力した際、Pythonスキルを持つエンジニアが上位に出現する(目視による定性確認) | M | IT | ✔︎ |

---

## 3. chain.py テストケース

| No. | テスト内容 | 優先度 | 種別 | 結果 |
|-----|------------------|----------|------|--------|
| C-01 | `build_prompt()` が案件テキストと候補リストを受け取り、空でない文字列を返却する | H | UT | ✔︎ |
| C-02 | `build_prompt()` の出力に案件要件テキストと候補エンジニア名が含まれる | H | UT | ✔︎ |
| C-03 | `generate_reasons()` がモックLLM使用時に文字列を返却する(OpenAI API呼び出しはモック化) | H | UT | ✔︎ |
| C-04 | `generate_reasons()` の出力テキストが空文字列ではない(実API呼び出しで検証) | M | IT | ✔︎ |
| C-05 | 生成テキストに候補エンジニア名またはスキルへの言及が含まれる(定性確認) | L | IT | ✔︎ |

---

## 4. app.py テストケース

| No. | テスト内容 | 優先度 | 種別 | 結果 |
|-----|------------------|----------|------|--------|
| A-01 | Streamlitアプリが正常起動し、タイトルとテキストエリアが表示される | H | ST | ✔︎ |
| A-02 | サイドバーの「インデックス構築」ボタン押下で成功メッセージと登録件数が表示される | H | ST | ✔︎ |
| A-03 | テキストエリアに案件要件を入力し「マッチング実行」を押下すると、候補3件とマッチング理由が表示される | H | ST | ✔︎ |
| A-04 | 実行中にローディングスピナー(`st.spinner` 等)が表示される | M | ST | ✔︎ |
| A-05 | 結果に類似度スコアが数値として表示される | M | ST | ✔︎ |

---

## 5. 結合テストケース

| No. | テスト内容 | 優先度 | 種別 | 結果 |
|-----|------------------|----------|------|--------|
| I-01 | インデックス構築 → マッチング実行のフル動作がエラーなく完了する | H | ST | ✔︎ |
| I-02 | インデックスを再構築した後の検索でも結果が正しく返却される(重複登録の問題が発生しない) | H | IT | ✔︎ |
| I-03 | 異なる3種類の案件要件テキスト(Pythonバックエンド・インフラ・PM系)を入力し、それぞれ異なる候補が返却される(検索結果の多様性) | M | ST | ✔︎ |
| I-04 | Streamlit Cloudデプロイ環境でも I-01 と同じ動作が確認できる | H | ST | ✔︎ |
| I-05 | API呼び出しの応答時間が規定範囲内に収まる(検索:3秒、生成:10秒) | M | ST | △ |

---

## 6. エラーケーステストケース

| No. | テスト内容 | 優先度 | 種別 | 結果 |
|-----|------------------|----------|------|--------|
| X-01 | `OPENAI_API_KEY` 未設定でアプリ起動時、エラーメッセージ E003 が表示される | H | ST | ✔︎ |
| X-02 | インデックス未構築でマッチング実行時、エラーメッセージ E002 が表示される | H | ST | ✔︎ |
| X-03 | テキストエリアが空のまま「マッチング実行」押下時、エラーメッセージ E001 が表示される | H | ST | ✔︎ |
| X-04 | 不正なAPIキー(形式は正しいが認証失敗)を設定した際、E003 または E004 エラーが表示される | M | ST | △ |
| X-05 | `engineers.json` が存在しない状態でインデックス構築実行時、エラーメッセージ E005 が表示される | M | ST | ✔︎ |
| X-06 | ChromaDB保存ディレクトリに書き込み権限がない場合に適切なエラーが表示される | L | ST | △ |

---

## テスト実施サマリ

| 項目 | 内容 |
|------|--------|
| 実施日 | 2026-05-10 |
| 実施者 | mag |
| 総テストケース数 | 31 |
| 合格 | 28 |
| 条件付き合格 | 3 |
| 不合格 | 0 |
| 未実施 | 0 |

**備考**

- UT/IT(12件)はローカル環境で pytest により実施。すべてモックを使用し、実API呼び出しは不要。
- ST(7件)は 2026-05-02 に Streamlit Cloud デプロイ環境で目視確認済。
- 全31件を実施済み。残課題は条件付き合格(△)の3件で、追加調査を要する。
  - I-05 △:応答時間合計は約11秒(生成上限内)だが、UIが検索と生成の各フェーズを区別表示しない。
  - X-04 △:エラーは確認できたが、想定の E003 ではなく E004 が表示された。原因調査が必要。
  - X-06 △:書き込み・実行権限がない場合のエラーは確認できたが、権限エラー特有のものか判別できない(エラー種別の分類なし)。
